"""
Functions to compute ray paths and travel times.
"""

def trace_ray(origin, angle, velocity_model):
    """
    Calculate ray path from origin with initial angle.

    Parameters
    ----------
    origin : tuple of floats
        Coordinates (x, z).
    angle : float
        Initial angle in degrees.
    velocity_model : callable
        Function or object returning velocity at (x, z).
    """
    pass
